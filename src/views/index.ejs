<!DOCTYPE html>
<html>
<head>
    <title>File Uploader</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
    <style>
        .hero-section {
            background: linear-gradient(135deg, #6B73FF 0%, #000DFF 100%);
            color: white;
            padding: 4rem 0;
            margin-bottom: 2rem;
        }
        .nav-link {
            transition: all 0.3s ease;
        }
        .nav-link:hover {
            transform: translateY(-2px);
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="bi bi-cloud-upload"></i> File Uploader
            </a>
            <div class="navbar-nav ms-auto">
                <% if (user) { %>
                    <span class="navbar-text me-3">
                        <i class="bi bi-person-circle"></i> <%= user.email %>
                    </span>
                    <a class="nav-link" href="/files">
                        <i class="bi bi-folder"></i> My Files
                    </a>
                    <a class="nav-link" href="/auth/logout">
                        <i class="bi bi-box-arrow-right"></i> Logout
                    </a>
                <% } else { %>
                    <a class="nav-link" href="/auth/login">
                        <i class="bi bi-box-arrow-in-right"></i> Login
                    </a>
                    <a class="nav-link" href="/auth/register">
                        <i class="bi bi-person-plus"></i> Register
                    </a>
                <% } %>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <div class="hero-section text-center">
        <div class="container">
            <h1 class="display-4 mb-3">Welcome to File Uploader</h1>
            <p class="lead mb-4">A secure and easy way to manage your files online</p>
            <% if (!user) { %>
                <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
                    <a href="/auth/register" class="btn btn-light btn-lg px-4 gap-3">Get Started</a>
                    <a href="/auth/login" class="btn btn-outline-light btn-lg px-4">Sign In</a>
                </div>
            <% } else { %>
                <a href="/files" class="btn btn-light btn-lg px-4">Go to My Files</a>
            <% } %>
        </div>
    </div>

    <!-- Features Section -->
    <div class="container mb-5">
        <div class="row g-4 py-5">
            <div class="col-md-4">
                <div class="card h-100 shadow-sm">
                    <div class="card-body text-center">
                        <i class="bi bi-shield-check text-primary display-4 mb-3"></i>
                        <h3 class="card-title">Secure Storage</h3>
                        <p class="card-text">Your files are encrypted and stored securely in our cloud storage.</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card h-100 shadow-sm">
                    <div class="card-body text-center">
                        <i class="bi bi-folder2-open text-primary display-4 mb-3"></i>
                        <h3 class="card-title">Easy Organization</h3>
                        <p class="card-text">Organize your files with folders and keep everything in order.</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card h-100 shadow-sm">
                    <div class="card-body text-center">
                        <i class="bi bi-share text-primary display-4 mb-3"></i>
                        <h3 class="card-title">Quick Access</h3>
                        <p class="card-text">Access your files from anywhere, anytime with our cloud storage.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>